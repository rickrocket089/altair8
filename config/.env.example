# Anthropic
ANTHROPIC_API_KEY=

# OpenAI (image generation only — Mateo's team avatars; also GPT-5.2 as a
# study subject in the multi-model behavioral scripts)
OPENAI_API_KEY=

# OpenAI, separate key, scoped to Sophie (team_leader) only -- her own
# reasoning runs on OpenAI, every other persona stays on Anthropic.
TEAM_LEADER_OPENAI_API_KEY=

# Google (Gemini — cross-model behavioral testing)
GEMINI_API_KEY=

# GitHub
GITHUB_TOKEN=
GITHUB_REPO=rickrocket089/altair8

# FTP
FTP_HOST=
FTP_USER=
FTP_PASSWORD=
FTP_PATH=

# Database
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=altair8
POSTGRES_USER=altair
POSTGRES_PASSWORD=

# Vector DB
CHROMA_HOST=chromadb
CHROMA_PORT=8000
